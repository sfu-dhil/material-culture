{% extends 'base.html.twig' %}

{% block title %}Edit Image{% endblock %}

{% block body %}
    <h1>Edit Image</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="form-group">
            <label class="col-sm-2 control-label">Current Image</label>
        <div class="col-sm-10">
            <img src="{{ asset(image_prefix ~ '/' ~ image.thumbnailPath) }}" class="img-thumbnail" />
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Update" class="btn btn-primary" />
            {% embed 'AppBundle:artefact/partial:link.html.twig' with {'artefact': artefact, 'text': 'Cancel'} %}
            {% endembed %}
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}
{% block javascripts %}
    {% include 'NinesEditorBundle:editor:widget.html.twig' %}
{% endblock %}
